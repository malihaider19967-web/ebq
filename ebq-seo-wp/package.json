{
  "name": "ebq-seo-wp",
  "version": "1.0.0",
  "description": "EBQ SEO WordPress plugin — Gutenberg sidebar, admin column, dashboard widget.",
  "license": "GPL-2.0-or-later",
  "private": true,
  "scripts": {
    "build": "wp-scripts build src/sidebar/index.js --output-path=build --output-filename=sidebar.js",
    "start": "wp-scripts start src/sidebar/index.js --output-path=build --output-filename=sidebar.js",
    "test": "jest"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.0.0"
  }
}
