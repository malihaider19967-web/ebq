*.log
.DS_Store
.env
.env.backup
.env.production
.env.dusk.local
.env.dusk.*
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/ebq-seo-wp/node_modules
/public/hot
/public/storage
/public/downloads/*.zip
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
/ebq-seo-wp
# The WordPress plugin is its OWN repo (github.com/.../ebq-wordpress-plugin),
# checked out here for packaging. Never commit it into this repo (581M, nested .git).
/ebq-wordpress-plugin
# Added by code-review-graph
.code-review-graph/
proxylist.txt

# Worker env template (secrets) + editor swap files — never commit
.env.worker
*.swp
.*.swp
