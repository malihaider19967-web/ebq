Role: Expert SEO Strategist and System Architect.

Objective: Extend current audit report Create a comprehensive "Recommendation Logic Engine" for a website auditing tool. I need a set of static, rule-based mappings that link specific audit data points to actionable advice for the customer.

Context: My system collects data on Metadata, Structure, Performance, and Technical SEO. I do not want to use AI to generate these insights on the fly. Instead, I need you to provide the "Extra Value" logic that I will hard-code into my reporting templates.

Task: For each of the following data points found in my audit reports, provide:

Threshold/Condition: What specific value or "Warning" status should trigger a recommendation?

Highlight Text: A concise explanation of why this matters (the "Extra Value").

Actionable Recommendation: The specific step the user should take.

Data Points to Map:

Metadata: Title Tag length (under/over 60), Meta Description length (120-160 range), and OpenGraph/Twitter tag presence.

Structure: Heading Order (e.g., skipped levels), Word Count (high vs. low), and Keyword Density percentages.

Technical: Compression status (None vs. Gzip/Brotli), Response Time (TTFB) thresholds (e.g., >500ms), and HTTPS status.

Engagement: Image count (specifically when word count is high but images are 0).

Advanced: Schema (JSON-LD) presence and Flesch Readability scores.

Output Format: Provide the logic in a table or structured list that my developers can easily translate into if/else statements for our reporting engine.

Why this works for your system
Consistency: Every customer who has "Skipped Heading Levels" will get the same high-quality explanation and fix.

Speed: Since the logic is hard-coded (e.g., if (wordCount > 1000 && imageCount == 0)), the report remains instant and doesn't rely on expensive or slow AI API calls during the audit.

Authority: It transforms a "Page Audit" into a "Consultancy Report" by explaining the why behind the numbers, like how a 7th-grade readability score is the "sweet spot" for gaming tools.

Quick Tip for your System:
You can use the Keyword Density data to provide a "Diversity Alert". If one keyword is over 3%, have your system automatically suggest 3-4 synonyms (LSI keywords) to help the user avoid "Keyword Stuffing" penalties.

Additional:
Also update the report download and email